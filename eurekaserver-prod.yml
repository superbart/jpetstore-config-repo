server:
  port: ${EUREKA_PORT}

eureka:
  server:
    enable-self-preservation: false
  client:
    serviceUrl:
      defaultZone: http://${OTHER_EUREKA_URI}/eureka/
      registerWithEureka: false
      fetchRegistry: false
